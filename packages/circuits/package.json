{
    "name": "circuits",
    "version": "1.0.0",
    "scripts": {
        "clean": "rm -rf *.zkey *.vkey.json *.wtns *_js/ *.r1cs",
        "build": "bash scripts/build.sh"
    },
    "devDependencies": {
        "circom2": "^0.2.16",
        "snarkjs": "^0.7.3",
        "circomlib": "^2.0.5"
    }
}